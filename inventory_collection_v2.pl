Please approve the file write — this creates `stp_audit.pl` in `/opt/NetAutoCommitter/`. The script audits Spanning Tree Protocol state on Cisco switches (STP mode, root bridge placement, blocking port counts, topology change history, per-VLAN root port table), which is not covered by any of the existing scripts in the repo.